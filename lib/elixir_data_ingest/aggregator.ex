defmodule ElixirDataIngest.Aggregator do
  @moduledoc """
  Group and aggregate records.
  """

  # Challenge 6:
  # Write a group_by/2 function that uses Enum.group_by/3
  # to group records by a chosen field (e.g., :region).

  # Challenge 7:
  # Write totals_by/2 using Enum.reduce/3 to sum amounts per group.
end
