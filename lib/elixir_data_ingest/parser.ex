defmodule ElixirDataIngest.Parser do
  @moduledoc """
  Parsing CSV input.
  """

  # Challenge 1:
  # Write a function that takes a raw CSV string and splits it into lines.
  # Handle empty or whitespace-only input using pattern matching & guards.

  # Challenge 2:
  # Refactor your parsing using the |> pipe operator to chain transformations.
end
