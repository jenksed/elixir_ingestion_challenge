defmodule ElixirDataIngest.Record do
  @moduledoc """
  Canonical data structure for a single, validated record.
  """

  # Challenge 3:
  # Define a struct with @enforce_keys.
  # Required fields: id, date, amount.
  # Optional: region.
  # Add a @type t for clarity.
end
