defmodule ElixirDataIngest.Pipeline do
  @moduledoc """
  Orchestrate Parser, Transformer, Validator, and Aggregator.
  """

  # Challenge 8:
  # Implement a run/2 function that:
  #   - Parses raw input into lines
  #   - Splits lines into cells
  #   - Transforms cells into records
  #   - Validates records
  #   - Aggregates results
  # Use a `with` expression to handle failures gracefully.
end
